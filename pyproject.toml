[project]
name = "stableshot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "GBisi", email = "giuseppe.bisicchia@phd.unipi.it" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "pandas>=3.0.3",
    "qsimbench>=0.1.1",
]

[project.scripts]
stableshot = "stableshot:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
